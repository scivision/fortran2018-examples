## Fortran CMake examples

This directory contains examples of using CMake with modern Fortran.
CMake is highly recommended for most Fortran programs, instead of messing around with arcane Makefiles directly.
